var CoolioApp = {
  Models:       {}, 
  Collections:  {},
  Views:        {},
  Router:       null,
  initialize:   function() {
    // //initialize collections
    // exhibitList = new this.Collections.ExhibitList();
    // escapeList = new this.Collections.EscapeList();

    // // clean any hashes from URL
    // window.location.hash = "";

    // // begin routing
    // new this.Router(exhibitList, escapeList);

    // // exhibitList.add(new ExhibitModel(3, "Lions"));
    // // exhibitList.add(new ExhibitModel(3, "Tigers"));
    // // exhibitList.add(new ExhibitModel(3, "Bears"));

    // Backbone.history.start();
  }
}